sudo: required
dist: trusty
language: java
addons:
    apt:
        packages:
        - expect
        - nmap
        - tidy
before_install:
   - pwd
install:
  - sudo make --jobs=2 build
  - sudo ./tmp/eXist-run.sh
script:
  - prove -v - < t/travis.txt
